name: CI - Release PR Validation

on:
  pull_request:
    branches:
      - "release/*"

jobs:
  ci:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v5


      - name: Lint
        run: echo lint

      - name: Test
        run: echo unit test

      - name: Build
        run: echo some final step
